
1. ensure the directory is set to right place
2. run the command using:
sudo python3 makeserver.py

for the injection, go into the XSS reflected and paste the code inside the "whats your name" textbox, then press submit.